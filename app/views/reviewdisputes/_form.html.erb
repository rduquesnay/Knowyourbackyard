<%= form_for(@reviewdispute) do |f| %>
  <% if @reviewdispute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reviewdispute.errors.count, "error") %> prohibited this reviewdipute from being saved:</h2>

      <ul>
      <% @reviewdispute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :review_id, value: @id %>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
