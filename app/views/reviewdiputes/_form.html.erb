<%= form_for(@reviewdipute) do |f| %>
  <% if @reviewdipute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reviewdipute.errors.count, "error") %> prohibited this reviewdipute from being saved:</h2>

      <ul>
      <% @reviewdipute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :review_id, value: @id %>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
