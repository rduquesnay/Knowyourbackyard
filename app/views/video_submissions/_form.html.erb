<%= form_for(@video_submission) do |f| %>
  <% if @video_submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@video_submission.errors.count, "error") %> prohibited this video_submission from being saved:</h2>

      <ul>
      <% @video_submission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :trail_id, value: @trail.id %>
  <p>
  <strong>Video Submission for: </strong>
  <%= @trail.name %>
  </p>
  <div class="field">
    <%= f.label :demo_link %><br>
    <%= f.text_field :demo_link %>
  </div>
  <div class="field">
    <%= f.label :about %><br>
    <%= f.text_area :about %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
